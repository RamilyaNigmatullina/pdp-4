<%= render_react_component("Chat", {
  chat: ChatSerializer.new(chat).as_json,
  currentUser: UserSerializer.new(current_user).as_json,
  interlocutor: UserSerializer.new(chat.interlocutor).as_json,
  messages: ActiveModel::Serializer::CollectionSerializer.new(messages, serializer: MessageSerializer).to_a,
  users: ActiveModel::Serializer::CollectionSerializer.new(users, serializer: UserSerializer).to_a
}) %>
